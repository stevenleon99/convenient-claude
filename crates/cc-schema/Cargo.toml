[package]
name = "cc-schema"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
pulldown-cmark = "0.11"
thiserror = "2"
chrono = { version = "0.4", features = ["serde"] }
